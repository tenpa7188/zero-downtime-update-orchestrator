name: Self Hosted Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: 実行場所を確認する
        run: |
          whoami
          hostname
          pwd
          uname -a