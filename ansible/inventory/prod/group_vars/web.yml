---
# SSM 経由で接続する（SSH キー・ポート22 不要）
ansible_connection: community.aws.aws_ssm
ansible_aws_ssm_region: "{{ aws_region }}"
ansible_user: ssm-user
ansible_aws_ssm_bucket_name: zduo-ansible-ssm

apache_version: "2.4.67-1~deb13u1+ubuntu22.04.1+deb.sury.org+1"
