---
- name: "{{ backup_pattern }} の古いバックアップを削除する（{{ backup_keep }}世代保持）"
  shell: |
    ls -t {{ backup_pattern }} 2>/dev/null | tail -n +{{ backup_keep | int + 1 }} | xargs rm -f
  changed_when: false
  become: true
  delegate_to: lb01
